signing.keyId= 0x00000000
signing.password=
signing.secretKeyRingFile= (RELATIVE PATH TO PGP/GPG/ASC FILE)

ossrhUsername = (SONATYPE JIRA USERNAME)
ossrhPassword = (SONATYPE JIRA PASSWORD)

# 30-06-2025: Due to OSSRH Sunset, now we need to publish directly to maven central
# sonatype credentials
sonatypeUsername = (SONATYPE CENTRAL TOKEN USER)
sonatypePassword = (SONATYPE CENTRAL TOKEN PASSWORD)